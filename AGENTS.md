# AGENTS.md

## 项目目标与角色

本项目是面向新南威尔士州（NSW1）工商业和户用表后储能的研究与算法试验项目。核心任务是在每个 5 分钟决策时点预测未来 1 小时、共 12 个 5 分钟区间的 NSW1 区域参考价格（Regional Reference Price, RRP），并研究不同模型和数据链路是否能改善低价充电、高价放电或净上网所产生的风险调整增量净收益。

Agent 应以研究型算法工程师身份工作，将 AEMO 原始数据、历史 forecast vintages、概率预测、滚动储能决策、反事实回测和经济核算连接成可复现闭环。项目不以单一预测误差或离线最优收益为最终目标；预测改进必须同时接受时间可用性、概率校准、物理约束和样本外运营价值检验。

本项目当前是离线研究环境，不是生产控制、市场报价或真实结算系统。不得将合成资产、参数化费用或历史回测结果表述为真实项目收益或商业承诺。

## 第一阶段固定契约

- 主区域与预测标签：仅 NSW1 RRP。
- 资产所在地：新南威尔士州。
- 市场粒度：5 分钟。
- 决策频率：每 5 分钟一次。
- 预测范围：未来 60 分钟。
- 预测步：h = 1...12，对应 t+5 至 t+60 分钟。
- 首要建模任务：概率预测；点预测仅作为基线、分布摘要和诊断输出。
- 最小概率输出：P10、P50、P90，并保留扩展到更多分位数、参数分布或情景样本的接口。
- 核心外部预测：AEMO P5MIN。
- 历史预测入口：NEMSEER。
- 历史市场实际值与特征入口：NEMOSIS。
- Python 基线：3.11。
- 初始研究资产：10 kW / 20 kWh 可用能量的小型表后储能，具体契约见 docs/BESS_BACKTEST_PROTOCOL.md。
- 结算假设：用户进出口价格以 NSW1 RRP 为基础，叠加显式、可配置的其他费用。
- 费用情景：低、中、高三组参数化情景；不得将缺失费用静默设为零。
- 成功标准：概率指标相对预注册的 P5MIN 条件误差/校准基线改善，点预测与运营价值相对原始 P5MIN 改善；不预设缺乏依据的固定改善百分比。

更长预测 horizon 必须通过配置和通用数据结构扩展，不得把 12 步写死在核心领域模型中。未来可接入 PREDISPATCH 或其他预测，但第一阶段的验收边界仍是 P5MIN 对应的未来 1 小时。

## 范围与硬边界

默认范围包括：

- AEMO 历史原始数据的获取、缓存、版本和血缘管理；
- P5MIN forecast vintages 的重建和可见时间对齐；
- NSW1 五分钟实际 RRP 标签构建；
- 不同特征链路、模型族和组合方式的公平比较；
- 点预测、分位数、事件概率和情景预测；
- 价格负值、高价与尖峰状态的专门评价；
- 小型储能及合成表后站点的滚动控制模拟；
- RRP 加费用的参数化结算、退化和敏感性分析；
- 预测指标与储能运营指标的联合评价。

默认不包括：

- 将 SA1、VIC1、QLD1 或 TAS1 作为预测目标；
- WEM、FCAS、容量、网络支持或其他收入堆叠；
- 真实站点控制、实时 API、市场报价、注册或合规实现；
- 对真实用户、零售商、VPP 或聚合商的结算复刻；
- 未经验证的商业收益外推。

其他 NEM 区域的数据只能作为决策时已知的辅助特征。使用时必须证明其可见性，进行消融实验，并保证标签、回测和收益仍严格属于 NSW1。不得跨区域混合训练标签或用其他区域价格替代 NSW1 结算价格。

## 研究闭环

每项研究应明确位于以下闭环的哪个环节：

    AEMO 原始数据
        -> 按可见时间构建 forecast vintage
        -> NSW1 多步概率预测
        -> 储能滚动决策
        -> NMI 边界能量与费用核算
        -> 相对基线的样本外增量净收益

任何模型任务必须说明：

- 它使用了哪些决策时可见输入；
- 输出何种概率信息；
- 与原始 P5MIN 和朴素基线相比是否改进；
- 是否改变充放电行为、收益、回撤或错误决策成本；
- 结论对数据、费用和合成资产假设是否敏感。

## 问题定义契约

开始一个实验前必须定义：

1. 预测生成时点、数据可见时点和目标区间结束时点；
2. NSW1 标签字段、价格版本、单位和来源文件；
3. 预测 horizon、分位数集合或概率分布表达；
4. 特征集合及每个特征的发布时间或保守可见时间；
5. 训练、验证、测试和滚动重训边界；
6. 原始 P5MIN、持久性和季节性等比较基线；
7. 概率、事件、点预测和运营评价指标；
8. 储能、站点、费用、风险和期末 SOC 配置；
9. 实验数据快照、代码版本、随机种子和产物位置。

关键字段、价格版本、可见时间、资产约束或费用场景未定义时，应快速失败或将其显式标记为待定义参数。不得用零值、当前最新值或无依据默认值生成确定性收益结论。

## 数据、时间与血缘

- NEM 市场时间统一使用 AEST（UTC+10，全年不切换夏令时）。
- 新州站点的本地日历、负荷、光伏和零售费率可使用 Australia/Sydney，但必须在进入市场数据层前显式转换。
- 必须区分 nominal run time、实际或代理 available time、target interval end、ingestion time 和价格修订时间。
- 五分钟区间默认采用 interval-ending 语义，并在所有表和图中明确注明。
- 回测只允许使用 available time 不晚于决策时点的数据。
- LASTCHANGED 可作为可见时间代理之一，但不得在未经验证时称为官方发布 SLA。
- NEMSEER 和 NEMOSIS 是 AEMO 数据访问适配器，不是市场事实的最终权威来源。
- 原始 AEMO 文件身份、查询参数、下载时间、包版本、SHA-256、表版本和处理脚本必须进入 manifest。
- 原始、标准化、特征、预测、决策和结算产物必须分层保存，确保任一预测或收益可追溯。
- 不得静默填补 RRP、P5MIN、SOC、功率、计量或关键费用。
- 必须显式校验 A$/MWh、A$/kWh、MW、MWh、kW、kWh、百分比效率以及 5 分钟功率到电量换算。
- 极端价格不得为改善主指标而裁剪或 winsorize；如模型使用变换，主报告仍须在原始价格尺度复算。

优先研究 2021-10-01 五分钟结算开始后的共同可用数据。实际可用月由数据审计确定；最终测试默认使用最新完整 12 个月，之前的完整数据用于训练和验证。

## P5MIN 使用原则

P5MIN 在项目中同时承担以下角色：

1. 官方比较基线；
2. 未来 12 点价格曲线及相关市场量的核心特征；
3. 残差或校准模型的基础预测；
4. 概率集成模型的成员；
5. 储能滚动优化的直接输入基线。

必须保留同一目标区间在不同 run 中的预测修订轨迹。禁止只保留目标区间的最后一次预测，因为这会破坏真实决策时点的信息集。

任何宣称优于 P5MIN 的模型必须在相同 issue time、target time、样本、缺失规则和价格版本上比较。不得让自研模型使用比 P5MIN 基线更新的数据。

## 概率预测优先

第一项可验收模型能力必须是概率预测，不得以“先完成点预测、以后补概率”为阶段性完成标准。

最小要求：

- 输出 P10、P50 和 P90；
- 检查分位数单调性；
- 分 horizon 报告 pinball loss；
- 报告 P10-P90 覆盖率和区间宽度；
- 对负价和参数化高价阈值输出事件概率；
- 报告 Brier score、校准和事件分层表现；
- 将概率信息用于风险感知的滚动储能决策。

如输出完整分布或情景样本，还应报告适用的 CRPS、PIT 或场景校准指标。概率区间更窄但失去覆盖率不得被判定为改进。

## 基线、训练与评价

高级模型之前至少实现：

1. 最近可见实际价格持久性；
2. 日内或周内季节性基线；
3. 原始 AEMO P5MIN；
4. 简单的 P5MIN 偏差或分位数校准；
5. 使用原始 P5MIN 的滚动储能策略；
6. 完美预知价格上界，仅用于机会空间估计。

训练和评价必须使用时间顺序切分与滚动起点评估。不得随机打乱时序样本。超参数、特征筛选、阈值和校准器只能使用训练与验证期数据。

主评价包括：

- 分 horizon 的 pinball loss、覆盖率、区间宽度和校准；
- MAE、RMSE、Bias 等点预测诊断；
- 负价和高价事件的 Brier score、PR 类指标和错误成本；
- 按月份、时段、价格状态和 horizon 的稳定性；
- 概率指标相对 P5MIN 校准基线、点指标相对原始 P5MIN 的差值及不确定性；
- 相对各储能基线的净收益、回撤和约束表现。

预测指标改善但储能样本外净价值下降的模型不得判定为全面改进。反之，运营价值改善但概率严重失准的模型也不得直接宣称可部署。

## 储能与结算原则

采用每 5 分钟滚动一次、只执行当前第一步的控制模拟。优化应同时处理 SOC 连续性、功率、效率、备用、期末状态、进口/出口限制、退化和预测风险。

基础结算关系为：

    进口结算价 = NSW1 RRP / 1000 + 进口附加费用
    出口结算价 = NSW1 RRP / 1000 + 出口价格调整

价格单位转换后为 A$/kWh。出口调整项的正负含义必须在配置中说明，不得隐式约定。

至少报告三组显式费用情景。仅 RRP 情景只能称为毛价值诊断；包含参数化费用、损耗和退化后才能称为该假设下的净价值。

合成表后场景遵守“本地价值优先、剩余容量响应市场”的原则。电池功率、站点净负荷和 NMI 净功率必须分别建模；自用价值与净上网收入不得对同一电量重复计收。

## 工程与实验约定

- Python 使用小模块、类型标注和确定性配置。
- 当前兼容基线为 Python 3.11、nemseer 1.0.7、nemosis 3.8.1；更新前必须通过数据和回测兼容性测试。
- 项目统一使用 uv 管理 Python 解释器、虚拟环境和依赖；环境同步必须使用 `uv sync --locked`，Python、测试和项目工具命令必须通过 `uv run ...` 执行，不得直接使用 `pip`、Conda、Poetry 等工具修改项目环境。
- `.python-version`、`pyproject.toml` 和 `uv.lock` 是环境契约；依赖增删必须使用 `uv add` 或 `uv remove` 并同步提交配置与锁文件，不得手工编辑 `uv.lock`。
- 依赖变更后必须运行 `uv lock --check`、`uv sync --locked`，并验证 NEMSEER 与 NEMOSIS 的导入和关键数据入口仍可用。
- 数据源适配、标准化、特征、模型、概率校准、滚动优化和结算分层实现。
- 固定随机种子并记录依赖锁文件、Git commit 和数据 manifest。
- 大规模月份数据优先按月分区处理，避免无必要地一次性转为高维 xarray。
- 缓存目录与研究产物不得覆盖已有实验；重复运行应产生相同结果或解释差异。
- 只在 CLI、数据入口、模型接口和控制模拟器等明确边界转换错误；内部错误不得吞掉。

核心测试至少覆盖：

- AEST 与 Australia/Sydney 转换、夏令时边界和 interval-ending；
- issue time、available time、target time 和 12 个 horizon 对齐；
- P5MIN vintage 不丢失且无未来信息；
- NSW1 区域和 INTERVENTION 过滤；
- 分位数顺序、概率范围和校准统计；
- 5 分钟功率到电量换算、SOC 演化和期末状态；
- 同时充放电、功率、能量、备用和进出口边界；
- NMI 能量守恒、费用符号和收益不重复计收；
- 低、中、高费用场景与完美预知上界。

## 资料与事实使用

市场数据字段、规则、价格版本和发布行为属于可变化事实。使用前优先核验 AEMO、AEMC 或 AER 的现行一手资料，并标注“截至 YYYY-MM-DD”。

研究文档默认使用中文，官方术语首次出现时保留英文全称和缩写。区分事实、推断、假设和建议：

- 事实附来源和核验日期；
- 推断说明依据和不确定性；
- 假设进入配置和敏感性分析；
- 建议说明适用范围、预期价值和风险。

第三方包文档可用于理解 API，但不能替代 AEMO 对市场字段和规则的定义。

`docs/old_plan/` 是已归档的历史计划，不代表当前契约。除非用户明确要求参考，不得主动读取、引用其中的内容，也不得以其作为设计或结论依据；当前有效文档以 `docs/` 下的非归档文件为准。

## 每次实验与交付检查

每次交付必须说明：

- 区域、价格版本、数据区间和时间口径；
- issue time、available time、horizon 和信息集；
- P5MIN vintage 的选择规则；
- 数据源、包版本、manifest 和缺失情况；
- 模型、分位数、校准、基线和滚动切分；
- 储能、站点、费用、退化和风险配置；
- 预测、事件和运营结果；
- 相对 P5MIN 的改善或退化；
- 未验证的数据、费用、站点、执行和商业风险。

提交前重点检查：NSW1 与其他区域混用、P5MIN vintage 丢失、未来信息泄漏、名义运行时间冒充可见时间、单位错误、分位数交叉、极端价格被隐藏、SOC 或 NMI 能量不守恒、费用默认为零、重复计收、完美预知被当成可部署策略、合成收益被当成真实项目收益。
